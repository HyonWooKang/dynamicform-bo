export type TrendPoint = {
  label: string;
  value: number;
};

export type PieSlice = {
  label: string;
  value: number;
  color: string;
};

export type HighlightMetric = {
  label: string;
  value: string;
  helper?: string;
};
